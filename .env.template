# ##############################################################################
# 🛡️  R2Vault Backend Configuration Template
# ##############################################################################
# Purpose: This file contains all the environment variables required to run 
#          the R2Vault backend service.
#
# 🚀 SETUP INSTRUCTIONS:
#   1. Copy this file to a new file named '.env' in the same directory.
#   2. Replace the placeholder values with your actual configuration.
#   3. Secure the '.env' file (never commit it to version control).
# ##############################################################################

# ------------------------------------------------------------------------------
# ☁️  CLOUDFLARE R2 SETTINGS [REQUIRED]
# ------------------------------------------------------------------------------
# Obtain these from: Cloudflare Dashboard > R2 > Manage R2 API Tokens
# R2_ENDPOINT: The S3 API endpoint for your R2 account (usually includes account ID)
# R2_BUCKET:   The name of the bucket where files will be stored
# R2_ACCESS_KEY / R2_SECRET_KEY: Your API credentials with Read/Write permissions

R2_ENDPOINT=https://<your-account-id>.r2.cloudflarestorage.com
R2_BUCKET=r2vault-storage
R2_ACCESS_KEY=your-access-key-here
R2_SECRET_KEY=your-secret-key-here

# ------------------------------------------------------------------------------
# 🐘  DATABASE SETTINGS [REQUIRED]
# ------------------------------------------------------------------------------
# DB_URL:      Connection string. If running via Docker, use the url as it is below.
# DB_USERNAME: Database user (default: admin)
# DB_PASSWORD: Database password (default: root)

DB_URL=jdbc:postgresql://db:5432/r2vault
DB_USERNAME=admin
DB_PASSWORD=root

# ------------------------------------------------------------------------------
# 🔒  SECURITY & JWT SETTINGS
# ------------------------------------------------------------------------------
# JWT_SECRET:           A strong, random 256-bit string used to sign tokens [REQUIRED]
# JWT_EXPIRATION:       Access token lifetime in milliseconds (default: 15 mins) [OPTIONAL]
# JWT_REFRESH_EXPIR_MS: Refresh token lifetime in milliseconds (default: 7 days) [OPTIONAL]

JWT_SECRET=generate-a-very-long-random-string-here-for-production-security
JWT_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=604800000

# ------------------------------------------------------------------------------
# 🌐  EXTERNAL TUNNELING [OPTIONAL]
# ------------------------------------------------------------------------------
# NGROK_AUTHTOKEN: Required ONLY if you want to expose the local app via ngrok
#                  Get yours at: https://dashboard.ngrok.com/get-started/your-authtoken

NGROK_AUTHTOKEN=your-ngrok-token-here
